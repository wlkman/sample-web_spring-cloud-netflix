server:
  port: 8901
spring:
  application:
    name: springbootapp
  #  cloud:
  #    config:
  #      uri: http://localhost:8801/
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: springcloudconfig